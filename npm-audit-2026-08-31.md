# npm audit — 2026-08-31

No patch/minor upgrades available this week (all outdated packages require major-version bumps).

## Security advisories (HIGH)

| Package | Finding |
|---|---|
| `next` | Multiple HIGH advisories in 9.3.4-canary.0 – 16.3.0 range — fixed in ≥ 16.3.0 (requires major bump from 14.x) |
| `brace-expansion` | DoS via exponential expansion — fix requires major bump |
| `glob` | Vulnerability in 10.2.0 – 10.4.5 — fix requires upgrade |
| `js-yaml` | Quadratic CPU via merge-key chains (4.0.0 – 4.3.0) — fix requires major bump |
| `nanoid` | Non-secure/custom generators loop indefinitely |
| `postcss` | XSS via `</style>`; path traversal via source maps |

All fixes require major-version bumps (out of scope for this automated run). Manual review recommended.
